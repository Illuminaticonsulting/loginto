# LogInTo — Configuration
# Copy this to .env and change the values

# Password to access your remote desktop (CHANGE THIS!)
ACCESS_PASSWORD=changeme123

# Server port
PORT=3456

# Session secret (random string for cookie signing)
SESSION_SECRET=change-this-to-a-random-string

# Screen capture settings
CAPTURE_QUALITY=60
CAPTURE_FPS=15
CAPTURE_SCALE=0.5

# Max failed login attempts before lockout
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_MINUTES=15
