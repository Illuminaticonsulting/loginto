# LogInTo — Dashboard Server Configuration
# Copy this to .env

# Server port
PORT=3456

# Session secret (random string)
SESSION_SECRET=change-this-to-a-random-string

# Max failed login attempts before lockout
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_MINUTES=15
